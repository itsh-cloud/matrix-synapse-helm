---
apiVersion: v2
name: matrix-synapse
description: Matrix reference homeserver
icon: https://matrix.org/images/matrix-logo.svg
appVersion: 1.159.0

type: application
# This chart has its own version line. It is published under our own name to
# our own registry, so it does not track the upstream chart version. appVersion
# tracks Synapse; FORK.md records which upstream commit this is based on.
version: 1.0.1
maintainers:
  - name: Alexander Olofsson
    email: ace@haxalot.com

# ITSH FORK CHANGE: the bitnami postgresql and redis subchart dependencies are
# removed. They were the only bitnami references that pulled a chart, and
# charts.bitnami.com is being wound down (the free images already moved to the
# bitnamilegacy archive). Use externalPostgresql and, if you run workers,
# externalRedis instead.
